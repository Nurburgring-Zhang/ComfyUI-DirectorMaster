# Python
__pycache__/
*.py[cod]
*.egg-info/

# 测试运行残留
tests/_archive_tmp/
*.tmp

# 归档输出（运行时生成）
output/

# 版本库运行时文件
_versions/
.store.lock

# eco 生态预案运行时产物 (dm_pack 注册表/参考素材台账/决策审计轨/快照桥/谱系 — 本地生成, 不入库; 不碰 aggregator/eco 源码与 tests)
dm_pack.json
dm_ref_ledger.json
dm_decision_log.jsonl
dm_versions_bridge.json
decision_log.jsonl
ref_lineage.jsonl
ref_ledger/
# R2A-04: 否定恢复第三方包入库面 (doctor 默认扫描 eco/packs/ — 运行时产物写用户
# out_dir, 入库面只有 eco/packs 第三方包目录; 否定行后置, 后行规则优先恢复)
!eco/packs/**
!eco/packs/**/ref_ledger/

# IDE/系统
.vscode/
.idea/
.DS_Store
Thumbs.db

# 测试输出产物
tests/*_results.json
# 审计证据存档例外 (随包发布 — README 实测口径的证据锚点)
!tests/ten_round_results.json
!tests/aigc_random_full_results.json
!tests/llm_resilience_results.json
!tests/load_isolation_results.json
!tests/storyboard_contract_results.json
!tests/contract_render_results.json

# benchmark 运行产物 (逐请求 CSV — 本地生成, 不入库)
benchmark/results/
.acs/
