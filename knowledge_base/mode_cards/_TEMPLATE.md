# 模式卡模板

复制本文件到 `knowledge_base/mode_cards/<slug>/<mode_id>.md`，逐节替换占位内容。
填写纪律与硬校验规则见 `SCHEMA.md`；写完自检：`python tools/sync_mode_index.py --node <你的节点>`。
frontmatter 键集封闭（8 键），五节 H2 标题逐字保留、顺序固定；删除全部 `<尖括号占位>` 与本注释段。

---
mode_id: <ascii-kebab-全局唯一-如-cine-rhythm-flash-cut>
node: <注册节点逐字-如-DirectorMasterCinematic>
name: <下拉显示名逐字-必须在该节点 manifest.creative 枚举中>
one_liner: <一句话 ≤40 字-说清该模式把输入变成什么-零空话>
applicable: [<形态1>, <形态2>]
intensity: <low|medium|high|adaptive 四选一>
style_tags: [<标签1>, <标签2>]
aliases: [<同义词1>]   # 可空: 无同义词时写 []
---

## 意图

<该模式存在的理由：用户在什么输入形态/创作目标下选它；选它之后输出与同节点相邻模式的本质差别
（一句话讲清"差别在哪"，不写"更强大更专业"式空话）。≤3 句。>

## 核心手法

<该模式真实执行的 2-4 个具体手法，每条与"节点映射"里的实现分支一一对应。写"做什么+怎么做到"，
不写形容词。示例句式："以 X 为骨架拆解输入场景，逐段注入 Y，再按 Z 规则约束镜头时长"。>

## 参数表

| 参数 | 典型值 | 越界后果 |
|---|---|---|
| <该模式真实消费的参数名（见节点 INPUT_TYPES）> | <典型取值> | <越界后的真实行为：回退到什么/降级为什么/直接空转> |
| <第二个参数> | <典型取值> | <真实行为> |
| <第三个参数> | <典型取值> | <真实行为> |

<!-- 有参模式 ≥3 行数据行；无参模式 ≥2 行，改列节点级共享参数（核心数据包/导演风格/场景描述/情绪基调）
     及其在该模式里的真实消费方式。 -->

## 已知坑

<从代码边界条件与既有测试断言提炼的真实坑：触发条件 + 表现 + 规避方法。可引用测试证据
（如 tests/test_all_modes.py 的执行断言、tests/d1_grammar_probe.py 的语法唯一性）。
确无坑可写时写"未发现"，禁止虚构或照抄他卡。>

## 节点映射

- 实现文件：<aggregator/xxx.py>
- 分支/函数：<TEMPLATES["<模式下拉显示名>"] / build() 中 <分支条件> / <函数名>()>
- 数据来源：<该模式消费的库/数据表，如 director_profiles_film.FILM_DIRECTORS_100；无则写"仅节点内置模板">
