---
id: NP-006
rule: 用户请求时长必须真实覆盖片长——先记录请求值再分桶, 输出总时长对预算偏差≤8% (常态<0.5%); 禁止静默把 45 分钟请求量化到 30 分钟桶; 拓扑时长重塑与取整残差摊回后仍须回归 ±1% 档断言。
precedent: V16.4 实测返工: 基座时长量化缺陷曾把用户请求 45 分钟量化到 30 分钟桶, 输出偏差最高 33%, 违背"目标时长输入 tooltip 总时长恒覆盖片长"的既有承诺; 修复为阶段+张力驱动时长权重重塑 + 三步精确闭合 (加权→归一→取整残差按比例摊回), 并在 Cinematic 记录分桶前的用户请求时长 (_requested_minutes) 作为拓扑引擎预算来源。回归双断言: ten_rounds T10 时长覆盖 ±1%、test_random_full_v16 多时长档 ≤8%。
self_check: 输出总时长 / 用户请求时长 = ? 偏差是否≤8%? 分桶逻辑是否吞掉了请求值 (先记录后分桶)? 极端档位 (8/15/45/115 分钟) 是否也被静默改档?
evidence_ref: docs/CHANGELOG.md:72 (45min→30min 桶、偏差 33% 的修复记录); aggregator/cinematic_studio.py:870-876 (_requested_minutes 记录+分桶); tests/ten_rounds.py:391-395 (±1%); tests/test_random_full_v16.py:111-112 (≤8%)
---

# NP-006 时长漂移 (duration drift)

## 规则
时长是用户显式输入, 引擎无权改写它。规则:
1. 先记录后分桶: 在任何桶归一/量化发生之前, 把用户请求值存入 `_requested_minutes` 并向下游传递 (拓扑引擎预算来源);
2. 闭合精度: 加权→归一→取整残差按比例摊回三步闭合, 任意请求时长总偏差实测 ≤8% 内、常态 <0.5%;
3. 断言双档: 长片核心包 90min 口径按 ±1% 守 (ten_rounds T10), 多时长档位 (8/15/30/45/60/90/115 分钟) 按 ≤8% 守 (test_random_full_v16);
4. 禁止静默改档: 若确需改档 (如 240min 上限), 必须在输出中显式标注, 不得只改数字不出声。

## 判例
V16.4 情节拓扑引擎提交的修复记录 (CHANGELOG): "修复基座时长量化缺陷——用户请求 45 分钟被量化到 30 分钟桶 (输出偏差最高 33%), 违背 目标时长 输入 tooltip'总时长恒覆盖片长'的既有承诺。新实现: 阶段+张力驱动时长权重重塑 (低张力长镜/高张力快切 + 确定性 jitter) + 三步精确闭合 (加权→归一→取整残差按比例摊回), 任意请求时长总偏差实测 ≤8% 内 (常态 <0.5%)。" 代码现场: cinematic_studio.py:716-722 注释 "V16.4: 记录分桶前的用户请求时长 (拓扑引擎时长归一的预算来源, 兑现'总时长恒覆盖片长')" 后紧跟 `_requested_minutes = target_minutes` 与分桶逻辑——记录先于分桶, 顺序即承诺。

## 自检
- 总时长秒 / (请求分钟×60): 偏差多少? 超过 8% 即返工。
- 分桶表 (≥110→120 / ≥80→90 / ≥50→60 / ≥20→30): 请求值是否在分桶前已存 `_requested_minutes`?
- 短时长档 (8/15/45 分钟) 是否也被静默改档?

## 证据指针
- docs/CHANGELOG.md:72 — V16.4 时长归一修复 (45min→30min 桶, 偏差 33%)
- aggregator/cinematic_studio.py:716-722 — _requested_minutes 记录 + 分桶 (先记录后分桶)
- tests/ten_rounds.py:391-395 — T10 "时长覆盖 ±1%" 断言 (默认 90min 核心包)
- tests/test_random_full_v16.py:111-112 — 多时长档 "时长归一≤8%" 断言
