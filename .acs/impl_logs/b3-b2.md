# b3-b2 实现日志（批次3 WaveB — 判例库 + 映射去重 + benchmark）

日期: 2026-08-31 · builder: b3-b2 · 基线: V16.6.0-MERGED (58ed138) · design: design_batch3.md §7(D7) §8(D8)

## 一、新增

1. `knowledge_base/quality_precedents/`（D7 判例库）
   - `__init__.py` 加载器：冻结接口 `list_precedents() -> list[dict]`；条目 8 键同构（id/rule/precedent/self_check/evidence_ref/file/ok/error）；字段完整率硬校验（五语料字段任一缺失→该条 ok=False 带原因进返回值，绝不静默跳过）；键集封闭（未知 frontmatter 键=错误条目）；id 口径 `^NP-\d{3}$` + 重复 id 检测；逐文件 try（坏文件不崩）；import 零 I/O 副作用；手写 frontmatter 解析（禁 yaml，对齐 sync_mode_index 口径）。
   - `NP-001`~`NP-012` 共 12 条语料（NP-001-face-drift / 002-fake-text / 003-subject-inconsistency / 004-vacant-words / 005-repetition / 006-duration-drift / 007-scene-entity-miss / 008-composition-repeat / 009-focal-mismatch / 010-canned-props / 011-meta-language-leak / 012-fake-success），覆盖 design §7 全部 12 类；每条 frontmatter 五字段 + 正文四节展开（规则/判例/自检/证据指针）。证据来源全部为仓库真实指针（file:line 或测试名）：ten_rounds T10 结构硬指标+一票否决+时长±1%、test_random_full_v16 相邻焦点≤15%+零AI套话+时长≤8%、d1/d2 探针红线（0.91 审计基线/同簇语法指纹）、test_matrix_full VACANT_WORDS+实体命中硬门+焦段匹配≥90%、final_capability_audit 86-91、asset_registry_data POST_ISSUE_LIST（face_drift:364/fake_text:367/face_and_hands_first:377）、scene_entity（focal_for_size:240-258/相邻运镜不重复:522/extract_entities/rewrite_focus）、cinematic_studio 实体优先接线:946-953、feature_film_engine 清洗词:3689/:3796、anti_ai_vocab:24-40（含 V13.5 合法规格词边界）、storyboard_contract 11 诊断码:17-21、CHANGELOG 判例行:32/38/49/50/51/72/85、mode_cards SCHEMA 红线:58-60 与 cinematic-viral-twist:34 卡面失实教训。NP-001/002 诚实标注"本地暂无实测返工判例，为生产分类学沉淀的预防性规则"（证据指针指向仓库内防线，未编造）。
2. `benchmark/`（D8 经济性基准）
   - `README.md`：三条硬规矩（固定任务×固定模型×逐请求记录；usage 从端点 usage 字段拉取、缺失留空永不估算、不算金额；无真实端点诚实标 MOCK）+ CSV 列定义 + 任务 schema + 退出码。
   - `tasks/` 3 个固定任务 JSON（B3-T1 机甲码头 15s 分镜 / B3-T2 父女厨房剧本 / B3-T3 手表广告分镜），各含 messages+参数+期望结构（必含键/每镜必含键/最少镜数/最少字数/必含标记/禁含词）。
   - `run_benchmark.py`：--endpoint/--model/--api-key-env（env 名，密钥不落盘）/--repeats/--timeout/--tag/--mock/--mock-mode；f2 式内嵌 OpenAI 兼容服务器；逐请求 CSV（19 列）落 `benchmark/results/`；REAL/MOCK 双口径摘要（p50/均值延迟、usage 汇总、结构检查通过数）。
3. `tests/test_mode_dedup.py`：31 断言（A 去重 11 + B 映射 10 + C 节点集成 8 + D 判例库冻结接口 3）。

## 二、修改

1. `aggregator/cinematic_studio.py`（**+171/−0，纯增量**，git numstat 佐证）：
   - INPUT_TYPES optional 末尾追加 `卖点清单`（STRING multiline；追加在末位保住既有工作流 widgets_values 对齐）；
   - 模块级新增 `_SP_SCAN_KEYS/_DEDUP_NOTE/_split_selling_points/_point_in_text/build_selling_point_map/render_selling_point_block/check_technique_dedup`；
   - build() 内：叙事编排定稿后计算 `_sp_map/_sp_block/_dedup_report`（卖点命中镜号=最终银幕序）；format_shot_table 后按需追加【卖点映射】段（无输入缺席）；`_storyboard_payload` 追加 `手法去重`（恒在）与 `卖点映射`（仅有输入时整键出现）两键，既有键零增删；契约 v1 将新键收进 normalized.extra（unknown-field 为 warning，不触发 stderr 错误门槛）。
   - 手法去重口径：同一运镜词/构图模板连续≥2 镜当主角各记 1 条（复合词取首段；连续段合并计 1 条）；只诊断不改写，分镜表逐镜零变更。
2. `.gitignore`：追加 `benchmark/results/` 忽略行。

## 三、验证结果（全部实测，输出存 scratch mt16bymxnxqc43b2/b3b2_*.txt）

- `tests/test_mode_dedup.py`：**31 PASS / 0 FAIL**（含 C7 同输入逐字节确定、C6 分镜表与基线逐镜一致、D 组冻结接口 12 条全 ok）。
- `tests/test_workflows.py`：**18 PASS / 0 FAIL**（B1 已并 18 节点状态下）。
- `tests/test_all_modes.py`（258 口径回归）：**280 PASS / 0 FAIL**（18 节点全模式 + Review 3 模式；B1 联动后新基线）。
- `tests/test_random_full_v16.py`：**73 PASS / 0 FAIL**（含"同种子拓扑 JSON 逐字节一致"——证明新增 JSON 键确定性）。
- T10 只读复刻（自建 scratch 脚本，未触发 ten_rounds 写 `tests/ten_round_results.json`）：**10 PASS / 0 FAIL**（4 理论结构硬指标/一票否决零命中/浮点伪影清零/时长覆盖 ±1%/D1/D2）。
- benchmark mock 通路：good 模式 6/6 请求、结构检查 **76/76**、CSV 落盘、exit 0；error 模式逐请求 ok=0 诚实记录、usage 留空、exit 1；无端点拒绝并给指引、exit 2。
- ast feature_version=(3,8)：cinematic_studio/test_mode_dedup/run_benchmark/quality_precedents 全过。
- 加载器负路径实测：缺字段/无 frontmatter/未知键 → 逐条 ok=False 带原因，不崩、不静默跳过。

## 四、未做与边界（诚实申报）

- 判例库 NP-001（脸崩）/NP-002（文字糊）为预防性规则（仓库无本地实测返工判例，证据指向 Higgsfield 生产问题单模板与提示词防线，已在卡内显式标注）——未编造判例。
- 未改 pln_llm/llm_engine（A1 冻结）、archive_master（A3）、mode_cards/manifest、__init__/doctor/README/pyproject（B1）；未跑全量回归矩阵（WaveC 职责）；未做任何 git 提交操作。
- `tests/ten_round_results.json` 未被触碰（T10 用只读复刻方式）。

## 跨界请求节

1. 【上报·契约 owner/WaveC 裁决】既有引擎-契约口径差异（**与 B2 增量无关，差分已证**）：`蒙太奇大师/一秒三闪/子弹时间/MV 慢镜` 4 模式触发契约 `invalid-duration` error（stderr 诚实上报，不失败节点）。差分实验：剔除 B2 新键后 4 模式错误码完全相同（b3b2_13_diff.txt）。归属分镜契约/时长生成口径，B2 无权修，请 WaveC 决定是否入修复闭环。
2. 【对 B1·已交付】冻结接口 `knowledge_base.quality_precedents.list_precedents()` 已实测可用：12 条全 ok、8 键同构、id NP-001 起、import 无副作用；test_mode_dedup D 组 3 断言钉住该接口。
3. 【对 WaveC】`ten_round_results.json` 未重写；全量回归并集请以 WaveC 重跑为准。
4. 无未决请求。
